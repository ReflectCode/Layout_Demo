<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.reflectcode.demo.ui.layout">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".MainScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".LinearLayout_Screen"> </activity>
        <activity android:name=".RelativeLayout_Screen"> </activity>
        <activity android:name=".FrameLayout_Screen"> </activity>
        <activity android:name=".ConstraintLayout_Screen"> </activity>
        <activity android:name=".TableLayout_Screen"> </activity>

    </application>

</manifest>